// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("hext", "for hext support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("hext", "enable hext support", "no");

if (PHP_HEXT != "no") {
	EXTENSION("hext", "hext.c");
}

